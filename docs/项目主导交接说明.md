# CalabiYau 项目主导交接说明

## 一、项目定位与协作目标

这是一个用于 Unity 游戏客户端/网络方向实习展示的个人项目：Unity 客户端加 .NET 8 C# UDP 独立服务器，实现服务端权威的第三人称坦克射击 Demo。

协作目标不是盲目堆叠“商业级”名词，而是让作者能理解、独立讲清并验证每项实现。作者是 Unity 初学阶段的本科生；讲解应先说明“解决什么问题、为什么选这个方案、有什么替代方案”，再开始代码修改。

当前 Unity 开发基线是 **Unity 2022.3.62f3c1 LTS**。项目已于 2026-08-28 从 2021.3.45f2c1 完成迁移，并于 2026-08-29 完成有画面的人工操作与双客户端验收；后续主工程只使用 2022.3.62f3c1 或经单独评估后的更高版本打开。阶段文档中出现的“Unity 2021.3 编译通过”是当时阶段验收的历史记录，不代表当前编辑器版本。完整迁移记录见 `docs/unity-2022-migration.md`。

## 二、已完成的网络底座

- UDP + JSON 通信；服务器固定 30Hz Tick，客户端只上报移动、瞄准和开火意图。
- 服务端权威 `GameWorld`：作为无渲染的 GameCore 式模拟核心，统一推进位置、朝向、血量、死亡和重生。
- 客户端本地预测、权威快照校正、未确认输入重放；远端快照插值、短时外推和表现层平滑追赶。
- 服务端权威射击：开火进入 Tick 队列，服务端维护玩家历史位置并进行有限延迟补偿；当前命中模型是简化的射线与圆形命中半径。
- 阶段 9 已完成服务端职责拆分：网络收发、连接会话、权威世界、快照构建、按客户端复制、可靠事件账本各自分离。
- 阶段 10 已完成命令时间线和权威校验门：输入去重、过期拒绝、输入租约、服务端推导射线和快照序列号。
- 阶段 11 已完成按客户端复制与轻量兴趣管理：距离与近期战斗状态决定复制范围和高/低频同步；当前是全量状态快照，不是真正字段差分。
- 阶段 12A 已完成开火请求序列、收据确认、有限重发和服务端去重。
- 阶段 12B/12B.1 已完成死亡、重生、击杀、结算的事件 ID、ACK、有限重传和去重；以 `lifeStateVersion` 解决死亡/重生乱序，Avatar 未创建时缓存生命周期事件；握手下发真实服务器 TickRate。
- 已有运行时调试面板与弱网验证能力：RTT、快照缓冲、预测误差、校正、快照缺口、开火收据、可靠事件重传和 ACK 延迟等。
- 阶段 16.0--16.3 已完成：共享二维碰撞数学、Tank 静态移动阻挡、服务器/预测/重放闭环、静态射击遮挡和合法出生/重生查询均已通过自动测试与人工验收。

## 三、已知边界：不能写成“已完成”

- 没有真正的基线确认、字段级差分复制、量化压缩或位打包；`changeMask` 仅为后续扩展预留。
- 当前兴趣管理是距离/战斗状态分级，不是视锥裁剪，也不是地图分区 AOI。
- 没有完整断线重连、观战回放、大厅匹配、独立战斗服调度或多线程世界分片。
- 当前已完成的是 Tank 的二维静态碰撞与查询，不是完整物理系统。服务器权威动态物体、人物 Capsule、斜坡、台阶与骨骼 HitBox 仍未实现，应在真实 3C 出现后继续。
- 当前表现仍是 Tank 验证版本，角色模型、第三人称相机、动画状态机和完整 3C 属于长期第三章方向。

## 四、当前建议的节奏

网络项目已具备较完整的实习展示价值。不要继续无止境扩张网络功能。

近期优先事项：

1. 阶段 16.0--16.3 与 Unity 2022 迁移均已完成人工验收；先保存可回退的 Tank 基线提交/标签、Build 和测试记录。
2. 人物主线从 `docs/3C开发陪练手册/00_总目录与共同决策规则.md` 进入。旧助手生成总纲、固定 S00～S38 路线和详细分卷已经归档且没有执行权；当前只进行 V00 愿景访谈 Q001。没有写入 `01_已确认事实与决策总账.md` 的内容，不得用于安装 Package、导入资源、修改 Unity 或编写代码。
3. 同步复习现有模块，准备架构图、README、弱网测试记录和演示视频；不能把服务器动态刚体、人物斜坡/台阶或骨骼 HitBox 写成已完成。

## 五、主导对话应遵守的约束

- 修改前先阅读相关代码和文档；不靠历史猜测覆盖当前实现。
- 只做小步、可运行、可验收的改动；每个阶段给出手动验收步骤和必要测试。
- 人物主线每轮必须先读取陪练手册中的当前 Sxx 课次；说明开发者要准备的资源，再给 Unity 菜单、Hierarchy、组件、字段拖拽和建议初值。代码完成后按输入、规则、输出、权威边界和调试点讲解。
- 每个 Sxx 和内部子步骤开始时只能先做只读检查，然后主动向开发者提问，比较候选方案并形成阶段决策卡。只有开发者明确确认实现内容与验收标准后，才能安装 Package、导入资源、修改场景/Prefab、写代码或改协议；助手不得以“默认方案”代替开发者决定。
- 未经开发者反馈 Unity 画面与人工操作结果，只能标记为“等待人工验收”，不得自动跳到下一课。
- 保持现有 UDP + JSON、服务端权威、固定 Tick 架构，除非先讨论迁移收益和代价。
- 不引入复杂框架、反射、ECS 或第三方网络库来制造“高级感”。
- 严格区分“已实现、正在验证、未来规划”，尤其在简历和项目文档中。
- 解释使用某项技术的原因和替代方案；代码优先清晰、适合作者复习。
- 尊重现有用户改动，禁止随意回滚、删除 Assets 或 Git 历史。

## 六、建议优先阅读的项目文档

1. `docs/3C开发陪练手册/00_总目录与共同决策规则.md`（人物主线活动入口）
2. `docs/3C开发陪练手册/01_已确认事实与决策总账.md`（唯一决策权威）
3. `docs/3C开发陪练手册/02_待访谈问题清单.md`（当前 Q001）
4. `docs/3C开发陪练手册/03_共同决策动态路线图.md`
5. `docs/3C开发陪练手册/CalabiYau第二部文档/已完成工程事实_2026-08-29.md`
6. `docs/项目复习指南.md`
7. `docs/模块一_服务器权威_Tick与命令时间线_复习笔记.md`
8. `docs/模块二_本地预测_校正与远端插值_复习笔记.md`
9. `docs/模块三_开火命中_延迟补偿与可靠战斗结果_复习笔记.md`
10. `docs/模块四_按客户端复制_兴趣范围与同步分级_复习笔记.md`
11. `docs/模块五_调试面板_弱网验证与测试复习笔记.md`
12. `docs/networked-tps-optimization-chapter.md`
13. `docs/stage-16-authoritative-collision-and-3c-foundation.md`

## 七、清理状态

2026-08-15 已删除旧恢复备份、Unity `Library` 缓存、.NET 构建缓存和无关输出文件。首次重新打开 Unity 会自动重建 `Library`，耗时较长属正常现象；源码、场景、文档、`Packages`、`ProjectSettings` 和服务器工程已核对保留。

2026-08-28 升级前另建了经过文件数量与哈希核对的 2021 副本：`D:\unity project\CalabiYau_2021_Backup_20260828`。主工程已在 2022.3.62f3c1 下重新导入、打开 `SampleScene`、完成引用扫描、Windows 开发包构建、服务端自动测试和真实 UDP 握手冒烟。该副本用于迁移回退，不是日常开发目录；不要与主工程同时打开。
